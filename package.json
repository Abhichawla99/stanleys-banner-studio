{
  "name": "stanleys-banner-poc",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "server": "node server/index.mjs",
    "client": "cd client && npm run dev",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "install-all": "npm install && cd client && npm install"
  },
  "dependencies": {
    "@google/genai": "^1.0.0",
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "multer": "^1.4.5-lts.1",
    "sharp": "^0.33.0",
    "concurrently": "^9.0.0"
  }
}
